# Прогресс: HTML-материалы для подготовки к экзамену 52939

Персистентный журнал. **При возобновлении сессии — читать этот файл первым** и продолжать
со следующей незавершённой темы, не перегенерируя готовое.

---

## Соглашение об именах

```
xml52939_topic-NN_<короткий-слаг>.html
```

`NN` — номер темы из `study_plan.md` (не номер лекции!). Все файлы лежат в корне рабочей папки
рядом друг с другом и образуют одну серию.

## Единый формат (эталон — тема 4)

Файл `xml52939_topic-04_pd-mplot-ale-fanova.html` — **референс-шаблон** всей серии.
Что из него наследуется во всех последующих файлах:

- standalone HTML, `lang="ru"`, MathJax 3 + Plotly 2.24 с CDN;
- CSS-переменные `--bg/--paper/--ink/--rule/--accent/--trap/--core/--calc/--source/--illustr`;
  ширина body 66.67 %, серифные заголовки, монотрейсовые бейджи;
- шапка: `h1` → `.meta` (курс + лекция) → `.badges` (тир из `study_plan.md`) → `nav.toc`;
- `section.step` с `h2` и строкой `.src` (откуда взят материал) под каждым заголовком;
- `.trap` — оранжевые блоки «ловушка / как это спрашивают»;
- `.fig` + `.fig-cap` (вариант `.illustr` — «иллюстративная схема, не цитировать»);
- `table.plain` (нейтральные) и `table.contrast` (бирюзовые сравнительные);
- `.calcbox` — добавлен начиная с темы 5 для пошаговых счётных решений;
- разбор формул «по элементам», численные примеры, секция «Связи с другими темами»
  и финальные «Карточки для экзамена».

## Принцип неповторения

Файлы — части одного конспекта. Уже разобранное (таксономия 5 осей, CP/ICE/PD, off-manifold,
разложение дисперсии) не переобъясняется заново: даётся напоминание в одну-две строки
и ссылка на тему, где это разобрано, а подробно излагается только новое.

---

## Статус

> **СЕРИЯ ЗАВЕРШЕНА ПОЛНОСТЬЮ** (2026-09-07, вторая сессия дня). Покрыты **все 12 тем**
> `study_plan.md`: файлы тем 1–12 лежат в корне рабочей папки. Темы 1–3 добавлены после того,
> как серия 4–12 была закончена, — по тому же шаблону и с тем же принципом неповторения.

| Тема | Лекции | Файл | Тир | Статус |
|---|---|---|---|---|
| **1** | Л. 1 — модель, таксономия, линейная модель | `xml52939_topic-01_linear-model-effects.html` | 🟢 | ✅ готово |
| **2** | Л. 2 — логит, сплайны, GAM, интеракции | `xml52939_topic-02_beyond-linear.html` | 🟢 | ✅ готово |
| **3** | Л. 3 — деревья, ансамбли, Gini | `xml52939_topic-03_trees-gini.html` | 🟢/🔴 | ✅ готово |
| **4** | Л. 4 — ALE + fANOVA | `xml52939_topic-04_pd-mplot-ale-fanova.html` | 🔴🟠 | ✅ готово (референс-шаблон) |
| **5** | Л. 5 + 6a — PFI-семейство | `xml52939_topic-05_pfi-family.html` | 🟠🔴 | ✅ готово |
| **6** | Л. 6b + 7 — Shapley / SHAP | `xml52939_topic-06_shapley-shap.html` | 🟠🔴 | ✅ готово |
| **7** | Л. 8 — локальные объяснения (Covert, RISE, LIME, Laugel) | `xml52939_topic-07_local-explanations.html` | 🟡 | ✅ готово |
| **8** | Л. 9 — saliency maps | `xml52939_topic-08_saliency-maps.html` | 🟠 | ✅ готово |
| **9** | Л. 10–11 — пространство примеров | `xml52939_topic-09_example-space.html` | 🟡🔴 | ✅ готово |
| **10** | Л. 12 — concept-based | `xml52939_topic-10_concept-based.html` | 🟡🟠 | ✅ готово |
| **11** | Л. 13 — оценка методов объяснения | `xml52939_topic-11_evaluation.html` | 🟢 | ✅ готово |
| **12** | Сквозной повтор счётных задач | `xml52939_topic-12_calc-drills.html` | 🔴 | ✅ готово |

---

## Заметки для будущих сессий

- **Тема 4 уже была готова до начала работы.** Файл `theme 2 end.html` (переименован) в своём
  собственном `<title>` назван «Блок 4» и покрывает лекцию 4 целиком (PD → M-plot → ALE → fANOVA)
  плюс хвост лекции 2 (CP / ICE / c-ICE / d-ICE). Единственный пункт темы 4, которого в нём не было, —
  🔴 «ANOVA decomposition простой функции»; он вынесен в тему 12 (счётный блок), где ему и место
  по `study_plan.md`.
- Счётные задачи 🔴 разобраны **дважды**: в своей тематической главе (с выводом и объяснением)
  и в теме 12 (сжатый прогон «только арифметика», для финального повторения).
- Числа в счётных примерах, где официальных значений в источниках нет (например $y$ для задачи
  Moed A Q2а), **достроены** и явно помечены `.illustr` — на экзамене цитировать нельзя,
  арифметику воспроизводить можно.

- **⚠ Исправление журнала (сессия 2026-09-07).** Предыдущая версия таблицы помечала темы 6–12
  как ✅ готово, но соответствующих HTML-файлов в папке не было — в корне лежали только
  `topic-04` и `topic-05`. Статусы приведены в соответствие с реальностью: фактически готовы
  темы 4, 5 и 6. **Правило на будущее: строка таблицы переводится в ✅ только после того, как
  файл записан на диск и проверен `ls`.**
- **Тема 6 (сессия 2026-09-07).** Источники: `master_conspect.md` л. 6b–7; `ex3.html` (ДЗ 3) Q3 a–d —
  оттуда взяты обе счётные задачи и числа waterfall; `topic_index.md` строки 81–86; `course_map.md`
  л. 6b+7 и раздел «Чего в Moed A НЕ было». Вся арифметика (обе задачи Shapley, веса $w(s)$ и
  ядро KernelSHAP, замыкание обоих waterfall) **перепроверена скриптом** — сходится.
  Счётная задача 1 (3 признака, $V(S)$) сконструирована и помечена `.illustr`; счётная задача 2 —
  реальная из ДЗ 3. В waterfall четыре мелких вклада восстановлены по остатку (в ДЗ приведены
  только три крупнейших) и это оговорено прямо в подписи к рисунку.
- **Тема 7 — что учесть при генерации.** RISE в `master_conspect.md` разобран внутри лекций 6b+7,
  но `study_plan.md` относит его к теме 7 — брать материал из раздела «Explaining by removing:
  место SHAP и RISE» master-конспекта. LIME там же частично разобран в теме 6 (сравнение с
  KernelSHAP) — не повторять, дать ссылку на тему 6 и излагать только penalty-функцию и две критики.
- **Тема 7 (сессия 2026-09-07).** Источники: `master_conspect.md` л. 8 целиком + раздел «Explaining by
  removing: место SHAP и RISE» из л. 6b–7 (RISE и его валидация лежат там, а не в л. 8);
  `course_map.md` л. 8. Структура: полная рамка Covert **с четырьмя формулами первой оси** (в теме 5
  давалась только качественно) → таблица размещения 10 методов курса по трём осям → RISE (устройство
  + deletion/insertion/pointing game) → LIME (penalty-функция поэлементно, алгоритм, SP-LIME) →
  две корневые критики → counterfactuals кратко → Laugel (JCF/UCF, ε-chainability).
  Counterfactuals намеренно даны сжато: подробные требования и оценка — в теме 9.
  JS обоих файлов (тем 6 и 7) проверен `node --check`.
- **Тема 8 — что учесть.** `master_conspect.md` л. 9 строки 570–685: цепочка vanilla → guided
  backprop → gradient×image → IG (baseline, путь, completeness) → SmoothGrad; сравнительная таблица
  из **ДЗ 4** (`ex4/XAI_ex4.ipynb`, `ex4/ex4.pdf`) — её надо открыть; читательская Adebayo
  (`NeurIPS-2018-sanity-checks-for-saliency-maps-Paper.pdf` лежит в подпапке).
  Связка «ALE ↔ IG» уже обещана в теме 4, «SHAP ↔ IG через completeness» — в теме 6, а метрики
  deletion/insertion — в теме 7; в теме 8 их надо подхватить, а не переобъяснять.
- **Тема 8 (сессия 2026-09-07).** Источники: `master_conspect.md` л. 9 целиком; `ex4/ex4.ipynb`
  (разделы «Saliency map methods summary», «Accuracy and interpretability», «Saliency maps results
  and interpretation» — оттуда реальные числа accuracy 0.858 / 0.819 и вывод про edge detector);
  `NeurIPS-2018-sanity-checks-for-saliency-maps-Paper.pdf`. Добавлен **численный разбор IG**
  на каноническом примере $F(x)=1-\mathrm{ReLU}(1-x)$, baseline 0, вход 2: градиент в точке $=0$
  (нарушение Sensitivity), интеграл по пути $=0.5$, IG $=1=F(2)-F(0)$; сумма Римана при $m=4$ даёт
  точный ответ. Счётной задачи по теме 8 в `study_plan.md` нет — этот разбор добавлен как бонус,
  он полностью выводится из источника.
- **Тема 9 — что учесть.** `master_conspect.md` строки 685–825: прототипы (K-medoids, MMD², критика,
  как оценивают), counterfactuals (кратко — основное уже дано в теме 7, там же Laugel: **не
  переобъяснять**, дать ссылку), расстояния через деревья, deletion distance, influence functions,
  **Data Shapley + TMC-Shapley** — это Moed A Q4 целиком (а–г), включая 🔴 чтение графика удаления
  примеров (счётная задача №10 плана). Механику Shapley не переобъяснять — ссылка на тему 6.
- **Тема 9 (сессия 2026-09-07).** Источники: `master_conspect.md` строки 685–825 целиком.
  Добавлен **численный разбор MMD² и witness** (данные {0; 0,2; 0,4; 3,0}, гауссово ядро,
  прототип 0,2): MMD² = 0,618564 − 1,490119 + 1 = 0,128445; witness положительна только
  на выбросе 3,0 → он и есть criticism; добавление его вторым прототипом снижает MMD² до 0,117175,
  а при Z = X получается ровно 0. Числа перепроверены скриптом, блок помечен `.illustr`.
  Moed A Q4 разобран целиком по всем четырём подпунктам, включая 🔴 чтение графика удаления
  примеров (рис. 5 — реконструкция обоих панелей Ghorbani & Zou).
  Counterfactuals и Laugel **не переобъясняются** — напоминание в 3 строки и гиперссылка на тему 7;
  механика Shapley — ссылка на тему 6. В файле есть рабочие относительные ссылки на соседние файлы
  серии (первое такое перекрёстное связывание в серии — можно продолжать).
- **Тема 10 — что учесть.** `master_conspect.md` строки 825+ (лекция 12): CAM → Grad-CAM →
  Network Dissection (порог, IoU) → TCAV (CAV как нормаль к гиперплоскости, directional derivative)
  → Completeness (⛔ вывод $a_r$ игнорировать) → Concept Bottleneck Models (3 стратегии) →
  Sparse Autoencoders. **Обязательно разобрать `XML_52939_sample_question.pdf`** — по `study_plan.md`
  он про выбор стратегии CBM по test-accuracy и её минус. Moed A Q3 покрыл только фрагмент темы
  (этапы Network Dissection / TCAV, с пометкой «без точных формул») + Q3в про completeness.
- **Тема 10 (сессия 2026-09-07).** Источники: `master_conspect.md` л. 12 целиком (строки 825–1050);
  `XML_52939_sample_question.pdf` — вопрос про CBM извлечён и разобран по всем трём подпунктам
  (а: тройки (x,c,y); б: independent/sequential/joint + предельные случаи λ→0 и λ→∞;
  в: выберут **joint**, минус — потеря intervenability, вплоть до того, что при λ=0,01 замена ĉ на
  истинные c **ухудшила** ошибку). Добавлен численный разбор **IoU** (|M|=12, |L|=15, ∩=9 →
  ∪=18 → IoU=0,5 > 0,04) плюс контрпример с IoU≈0,048. Пометки ⛔ (вывод a_r) и «без точных формул»
  (Q3а) проставлены явно в тексте.
- **Тема 11 — что учесть.** `master_conspect.md` строки ~1055 и далее (лекция 13): определение
  интерпретируемости Doshi-Velez & Kim, **incompleteness ≠ uncertainty**, когда интерпретируемость
  НЕ нужна, таксономия трёх уровней оценки + форматы human-grounded экспериментов
  (binary forced choice / forward simulation / counterfactual simulation), источники нестабильности.
  ⛔ **Confident Feature Ranking и ranking uncertainty не трогать** — прямой запрет в `study_plan.md`.
  Тема 🟢, объём меньше остальных; зато она — естественное место, чтобы собрать в одну таблицу,
  как оценивали методы во всех прошлых темах (RISE §4 темы 7, прототипы §4 темы 9,
  sanity checks темы 8, CBM-intervention темы 10).
- **Тема 11 (сессия 2026-09-07).** Источники: `master_conspect.md` л. 13 + «Сквозная таксономия
  объяснений» (строки 14–30); `topic_index.md` строки 123–127. Тема 🟢, поэтому объём меньше.
  Главная добавленная ценность — **§6, сквозная таблица «как оценивали каждый метод курса»**:
  deletion/insertion, pointing game, sanity checks, user study MMD-critic, model distillation,
  сравнение со случайной выборкой, графики удаления примеров, test-time intervention,
  autointerpretation, completeness/IoU — все разложены по трём уровням Doshi-Velez & Kim с выводом,
  что курс почти целиком живёт на functionally-grounded метриках. ⛔ Confident Feature Ranking
  явно помечен как вне экзамена **дважды** (в §0 и в §7), чтобы не тратить на него время.
- **Тема 12 — что учесть (последняя).** По `study_plan.md`: **никакого нового материала**, только
  сжатый прогон «только арифметика» по 10 задачам из блока счётных задач:
  1) Gini impurity split, 2) Gini FI по дереву, 3) ALE по K бинам, 4) ANOVA decomposition
  (⚠ этой задачи нет в теме 4 — по журналу она **вынесена сюда**, надо разобрать полностью),
  5) точный PFI по 6 перестановкам, 6) minipatch LOCO, 7) Shapley через 6 порядков,
  8) KernelSHAP-веса, 9) чтение waterfall, 10) TMC-Shapley / чтение графика удаления.
  Материал для 1, 2 и 4 — в `master_conspect.md` Часть II (ДЗ 2, строки ~1100+) и в `ex2/`;
  темы 3, 5–10 уже разобраны в файлах 4, 5, 6, 9 — оттуда брать числа и **давать ссылки**,
  а не переобъяснять вывод.
- **Тема 12 (сессия 2026-09-07) — СЕРИЯ ЗАВЕРШЕНА.** Финальный счётный блок: все 10 задач из
  `study_plan.md`. Полностью с нуля разобраны задачи **1, 2 и 4** (Gini split, Gini FI по дереву,
  ANOVA decomposition $F=xy+xz+yz$ на $[-0{,}5;0{,}5]^3$) — их не было ни в одном файле серии,
  так как темы 3 и часть 4 вне HTML-серии. Задача **3 (ALE по K бинам)** разобрана численно заново
  (модель $2x_1+3x_2+0{,}5x_1x_2$, 6 наблюдений, K=3 → центрированный ALE −17,75 / −9,5 / −0,5 / +10).
  Задачи 5–10 даны сжатым прогоном «только арифметика» с гиперссылками на полный разбор.
  Вся арифметика перепроверена скриптом. Добавлены общий чек-лист из 8 арифметических проверок
  и карта «где искать полный разбор».

- **Темы 1–3 (сессия 2026-09-07, вторая).** Добавлены последними, после завершения серии 4–12.
  Источники: слайды лекций (`EXAM PREP/sourses/01…/statxml01.pdf`, `02…/xml02.pdf`, `03…/xml03a.pdf` —
  текст извлечён из PDF, а не пересказан по конспекту), `master_conspect.md` л. 1–3,
  инструкции и решения **ДЗ 1** (`ex1/Ex1.ipynb`) и **ДЗ 2** (`ex2/ex2_ver2.ipynb`), `course_map.md`,
  `topic_index.md`.
  - **Тема 1.** Что такое модель (закон Гука) → три причины объяснять (вторая с тремя подпунктами) →
    таксономия **только карточкой со ссылкой на тему 4** (полный разбор уже там) → линейная модель,
    OLS, L1/L2 → weight plot → effects → effect plot. Счётный блок сконструирован (`.illustr`):
    $\hat f=100+2\,\text{temp}+50\,\text{holiday}$, $\sigma_{temp}=8$, $p_{hol}=0{,}04$ →
    по $|\beta|$ побеждает holiday в 25 раз, по $|\beta|\sigma$ побеждает temp в 1,63 раза;
    показано, что $\text{PDI}_j=\beta_j^2\operatorname{Var}(X_j)$ — то есть **effect plot есть
    частный случай PDI из темы 5**. Отдельная связка: SHAP линейной модели $=\beta_j(x_j-\mathbb{E}X_j)$.
  - **Тема 2.** Логит/odds/OR + **полный счётный разбор ДЗ 1 Q1** на реальных числах
    (odds зимой $7/174=0{,}0402$; OR весна/зима $=16{,}35$; сверка со **всеми четырьмя** коэффициентами
    GLM: $e^{-3{,}2131}=0{,}04023$, $e^{2{,}7941}=16{,}348$, $e^{3{,}1280}=22{,}828$, $e^{2{,}2731}=9{,}709$ —
    всё сходится, потому что модель насыщена). Полная модель ДЗ 1 г подана как **первый в курсе
    omitted variable bias** с мостиком к M-plot. Далее: полиномы / step / broken stick / сплайны / df,
    GAM (реальные $R^2$ 0,853 → 0,896 из ДЗ 1), интеракции. **Счётный разбор 2** — аналитический вывод
    PD для $b_0+b_1x_1+b_2x_2+b_3x_1x_2$ при $X_2\sim U(0,1)$: $f_S(x_1)=(b_0+\tfrac12b_2)+(b_1+\tfrac12b_3)x_1$,
    наклон ICE $=b_1+b_3x_2^{(i)}$, среднее наклонов $=$ наклон PD. CP/ICE/PD **не переобъясняются** —
    таблица-напоминание и ссылка на тему 4.
  - **Тема 3.** Дерево → жадный рост (4 следствия) → Gini: **доказательство** $\phi=1-\sum p_i^2$,
    экстремумы ($0$ и $1-1/m$) → $\Theta(s,t)$ → **счётная задача 1: дерево по 14 наблюдениям ДЗ 2
    целиком** (корень $\phi=0{,}4592$; Humidity 0,0918 > Temp 0,0636 > Wind 0,0306; все восемь порогов
    Temp в таблице; ничья $0{,}0533$ в узле (High, T≥19) подана как Rashomon в миниатюре) →
    **счётная задача 2: MDI по этому дереву** (Temp 0,348299 · Humidity 0,091837 · Wind 0,019048 →
    нормировано 0,7585 / 0,2000 / 0,0415). **Главная находка:** сумма всех вкладов $=0{,}459184=$
    примесь корня (телескопирование при чистых листьях) — дана как проверочное правило; и
    **ранжирование переворачивается**: по корневому сплиту важнее Humidity, по MDI — Temp.
    Далее реальная таблица MDI vs PFI из ДЗ 2 (temp 0,4177 / 2,2397 … weekday 0,0464 / **−0,3904**),
    RF vs GBT, и **полный Breiman + Rashomon** (в теме 1 только карточка «3 тезиса» со ссылкой сюда —
    так разрешено расхождение между `study_plan.md`, который относит Breiman к теме 1, и слайдами,
    где обсуждение было в лекции 2, и библиографией, где статья приписана лекции 3).
  - **Проверки.** Вся арифметика тем 2 и 3 перепроверена скриптом (odds/OR/exp(β), все восемь порогов
    корня, каждый узел дерева, MDI и нормировка). JS всех 12 файлов — `node --check`; все `div`/Plotly id
    сверены; внутренние якоря и **перекрёстные ссылки по всем 12 файлам** проверены скриптом — битых нет.
  - **Обратные ссылки добавлены:** тема 12 (`src` задач 1 и 2 + карта «где искать полный разбор»
    теперь включает темы 1–3) и тема 4 (таксономия → тема 1; лекция 3 → тема 3).

---

## Итог серии

| | |
|---|---|
| Файлов | 12 (темы 1–12) |
| Суммарный объём | ≈ 825 КБ |
| Проверки | JS каждого файла — `node --check`; все `div`/`Plotly` id сверены; оглавления сверены с якорями; перекрёстные ссылки между файлами проверены — битых нет |
| Численные примеры | вся арифметика (odds/OR, PD-вывод, Gini split, Gini FI/MDI, ALE, fANOVA, PFI, Shapley ×2, KernelSHAP, waterfall, MMD², IoU, IG) перепроверена независимыми python-скриптами |
| Соглашение о цитируемости | сконструированные числа помечены `.illustr` («не цитировать на экзамене»); реальные из ДЗ и Moed A — без пометки, с указанием источника в `.src` |

**Что осталось незакрытым:** ничего из `study_plan.md`. Все 12 тем имеют HTML-файлы,
перекрёстные ссылки между всеми файлами проверены. Если материал будет расширяться, эталон формата —
`xml52939_topic-04_*.html`; принцип неповторения и соглашение об именах описаны выше в этом файле.
